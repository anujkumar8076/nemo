# Sandbox manager (Phase 4)

This service is intentionally not implemented during foundation work. It will
provision disposable non-root workspaces without mounting the host Docker socket
into API, worker, web, or agent containers.
